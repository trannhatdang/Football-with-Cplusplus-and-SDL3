A top-down football game built with SDL3 and a C++ game engine built in the ECS Design Pattern.

The Engine has Scene, GameObject and Components. One Component runs OnIterate() every frame (similar to Update() in Unity). The engine has many other features similar to Unity in this vein.
